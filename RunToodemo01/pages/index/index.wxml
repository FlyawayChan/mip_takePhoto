<!--index.wxml-->
<view class="container">
  <button class="clickbtn" type="primary" size="mini" bindtap="clickMe">点</button>
  <dialog id="alertview"
          
          bind:cancelEvent='cancelEvent'
          bind:confirmEvent='confirmEvent'>
  </diglog>
  <!-- <view>姓名：{{userName}}</view>
  <view>年龄：{{age}}</view>
  <view wx:if="{{age > 17}}">您可以进入酒吧</view>
  <view wx:for="{{lists}}" wx:key="id">
      <switch>{{item.name}}</switch>
  </view>
  <button bindtap="tapEvent">更新</button>
  <view wx:for="{{[1,2,3]}}">{{item}}</view> -->
</view>